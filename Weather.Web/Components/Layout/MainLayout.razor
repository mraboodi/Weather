@inherits LayoutComponentBase
@using Weather.Web.Interfaces.Identity
@inject IAuthStateService AuthState
@inject NavigationManager Nav

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>      
        @*  @if (AuthStateService.UserHasAnyRole(UserRoles.SuperUser, UserRoles.Administrator))
        {
            <div class="top-row px-4">
                Welcome, @AuthStateService.GetLastName()?.ToUpper() @AuthStateService.GetFirstName().
            </div>
        } *@
        <article class="content p-0" style="padding: 0 !important;">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>


